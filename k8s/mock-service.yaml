apiVersion: v1
kind: Service
metadata:
  name: mock-a-service
spec:
  type: ClusterIP
  selector:
    app: mock-a
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: mock-b-service
spec:
  type: ClusterIP
  selector:
    app: mock-b
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080